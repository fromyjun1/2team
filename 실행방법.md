# 실행 방법

## 1. Oracle DB 설정

SQL*Plus 또는 SQL Developer에서 **순서대로** 실행:
```sql
@sql/part1_users.sql
@sql/part2_clubs.sql
@sql/part3_matching.sql
@sql/part4_community.sql
```

`application.properties`에서 DB 접속 정보 수정:
```properties
spring.datasource.url=jdbc:oracle:thin:@localhost:1521:XE
spring.datasource.username=your_username
spring.datasource.password=your_password
```

---

## 2. 백엔드 실행 (Spring Boot)

```bash
cd backend
mvn spring-boot:run
```
→ http://localhost:8080 에서 실행됨

---

## 3. 프론트엔드 실행 (React + Vite)

```bash
cd frontend
npm install
npm run dev
```
→ http://localhost:3000 에서 실행됨

---

## 주요 페이지 URL

| 경로 | 설명 |
|------|------|
| /login | 로그인 |
| /signup | 회원가입 (→ 태그 선택으로 이동) |
| /tags | 관심 태그 선택 |
| /mypage | 마이페이지 |
| /clubs | 동아리 목록 (카드 갤러리) |
| /clubs/:id | 동아리 상세 + 신청 |
| /clubs/:id/qna | 동아리 Q&A |
| /recommend | AI 태그 매칭 추천 결과 |
| /wishlist | 찜 목록 |
| /applications | 신청 현황 |
| /admin | 동아리 등록 (관리자) |

---

## API 엔드포인트 요약

| 파트 | Method | URL |
|------|--------|-----|
| 1 | POST | /api/users/signup |
| 1 | POST | /api/users/login |
| 1 | GET/PUT | /api/users/{id}/tags |
| 2 | GET | /api/clubs |
| 2 | GET | /api/clubs/{id} |
| 2 | POST | /api/clubs |
| 3 | GET | /api/match/{userId} |
| 3 | POST/DELETE | /api/match/{userId}/wishlist/{clubId} |
| 4 | POST | /api/applications |
| 4 | GET | /api/applications/user/{userId} |
| 4 | PATCH | /api/applications/{id}/status |
| 4 | GET/POST | /api/qna/club/{clubId} |
