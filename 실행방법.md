# 실행 방법

## 1. Google Cloud SDK 설치 및 인증 (최초 1회)

1. [https://cloud.google.com/sdk/docs/install](https://cloud.google.com/sdk/docs/install) 에서 Windows 설치 파일 다운로드 후 설치
2. 터미널에서 구글 계정 인증:
```bash
gcloud auth application-default login
```
브라우저가 열리면 팀 구글 계정으로 로그인

---

## 2. Cloud SQL Auth Proxy 실행 (백엔드 실행 내내 켜두기)

1. [cloud-sql-proxy 다운로드](https://github.com/GoogleCloudPlatform/cloud-sql-proxy/releases/download/v2.20.0/cloud-sql-proxy.windows.amd64.exe) 후 파일명을 `cloud-sqlpproxy.x64.exe`로 변경
2. PowerShell에서 실행 (창을 닫으면 DB 연결 끊김):
```powershell
& "C:\Users\[내PC이름]\cloud-sqlpproxy.x64.exe" project-42919437-c0df-4eec-804:asia-northeast3:club-db --port=5432
```
> **내PC이름 확인:** `echo $env:USERNAME`
> 
> ⚠️ 로컬에 PostgreSQL이 설치되어 있어 5432 포트가 사용 중이면 `--port=5433` 으로 실행하고,  
> `application.properties`의 DB URL도 `localhost:5433`으로 변경하세요.

→ `The proxy has started successfully` 메시지가 나오면 성공

---

## 3. 백엔드 실행 (Spring Boot)

```bash
cd backend
mvn spring-boot:run
```
→ http://localhost:8080 에서 실행됨

---

## 4. 프론트엔드 실행 (React + Vite)

```bash
cd frontend
npm install
npm run dev
```
→ http://localhost:3000 에서 실행됨

---

## 주요 페이지 URL

| 경로 | 설명 |
|------|------|
| /login | 로그인 |
| /signup | 회원가입 (→ 태그 선택으로 이동) |
| /tags | 관심 태그 선택 |
| /mypage | 마이페이지 |
| /clubs | 동아리 목록 (카드 갤러리) |
| /clubs/:id | 동아리 상세 + 신청 |
| /clubs/:id/qna | 동아리 Q&A |
| /recommend | AI 태그 매칭 추천 결과 |
| /wishlist | 찜 목록 |
| /applications | 신청 현황 |
| /admin | 동아리 등록 (관리자) |

---

## API 엔드포인트 요약

| 파트 | Method | URL |
|------|--------|-----|
| 1 | POST | /api/users/signup |
| 1 | POST | /api/users/login |
| 1 | GET/PUT | /api/users/{id}/tags |
| 2 | GET | /api/clubs |
| 2 | GET | /api/clubs/{id} |
| 2 | POST | /api/clubs |
| 3 | GET | /api/match/{userId} |
| 3 | POST/DELETE | /api/match/{userId}/wishlist/{clubId} |
| 4 | POST | /api/applications |
| 4 | GET | /api/applications/user/{userId} |
| 4 | PATCH | /api/applications/{id}/status |
| 4 | GET/POST | /api/qna/club/{clubId} |
