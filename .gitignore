# ── 백엔드 (Spring Boot / Maven) ──
backend/target/
backend/.mvn/
!backend/.mvn/wrapper/maven-wrapper.jar
backend/*.class
backend/*.jar
backend/*.war

# ── 프론트엔드 (React / Node) ──
frontend/node_modules/
frontend/dist/
frontend/.env
frontend/.env.local

# ── DB 접속 정보 ──
# application.properties 는 팀 공용 설정으로 git에 포함 (포트 변경 등 개인 설정은 로컬에서만 수정)
backend/src/main/resources/application-prod.properties

# ── IDE ──
.idea/
*.iml
.vscode/
.DS_Store
Thumbs.db

# ── 로그 ──
*.log
logs/

# ── Java 기타 ──
*.class
*.nar
*.ear
*.zip
*.tar.gz
*.rar
.mtj.tmp/
hs_err_pid*
replay_pid*
